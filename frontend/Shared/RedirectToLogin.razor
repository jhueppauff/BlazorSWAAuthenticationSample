@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"/.auth/login/github?post_login_redirect={Uri.EscapeDataString(Navigation.Uri)}", true);
    }
}